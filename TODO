Things to fix:

* Cross-compiling... check CROSS=, CROSS_COMPILE and other friends
  get set in the right places.
* Debuglevel

Modules to merge in:

* kernel {subtargets done, dependencies need cleaning up or checking}
  -> genzimage
  -> MIPS embedded image
* grub [gen_bootloader.sh]
* epatch function

* reorganize menu
* initramfs allow skipping of any cpio

* setup all module
